
import { weather } from 'emberjs-weather/helpers/weather';
import { module, test } from 'qunit';

module('Unit | Helper | weather');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weather([42]);
  assert.ok(result);
});

